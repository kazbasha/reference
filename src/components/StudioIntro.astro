---
---

<section class="studio-intro">
  <p class="studio-intro__text">Reference is an independent creative studio built for ambitious brands that want to <span class="nowrap">be the reference.</span></p>
  <p class="studio-intro__sub">Run like a band. We stay close to the work from first idea to final execution, creating brands, campaigns, films, content, experiences and original products... for clients and ourselves.</p>
</section>

<style>
  .studio-intro {
    background: #e8e4de;
    min-height: 100svh;
    padding: clamp(60px, 10vw, 140px) clamp(24px, 7vw, 100px);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: clamp(12px, 1.5vw, 22px);
  }

  .studio-intro__text {
    margin: 0;
    font-family: "ObviouslyWide", system-ui, sans-serif;
    font-size: clamp(22px, 3.6vw, 58px);
    font-weight: 400;
    line-height: 1.2;
    color: #1a1a1a;
    max-width: 26em;
    text-align: center;
  }

  .studio-intro__text .nowrap {
    white-space: nowrap;
  }

  .studio-intro__sub {
    margin: 0;
    font-family: "ObviouslyWide", system-ui, sans-serif;
    font-size: clamp(11px, 0.85vw, 14px);
    font-weight: 400;
    line-height: 1.65;
    color: rgba(26, 26, 26, 0.55);
    max-width: 52ch;
    text-align: center;
  }
</style>
