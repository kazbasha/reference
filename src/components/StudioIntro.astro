---
---

<section class="studio-intro">
  <p class="studio-intro__text">Reference is an independent creative studio run like a band and built for work that <span class="nowrap">sets the benchmark.</span></p>
  <p class="studio-intro__sub">Entertainment sits at the core of everything we do. From strategy to music-driven campaigns, social content to cinematic brand films, if it ain't entertaining, it doesn't sell. Because no one's waiting for another boring campaign.</p>
</section>

<style>
  .studio-intro {
    background: #e8e4de;
    min-height: 100svh;
    padding: clamp(60px, 10vw, 140px) clamp(24px, 7vw, 100px);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: clamp(12px, 1.5vw, 22px);
  }

  .studio-intro__text {
    margin: 0;
    font-family: "ObviouslyWide", system-ui, sans-serif;
    font-size: clamp(22px, 3.6vw, 58px);
    font-weight: 400;
    line-height: 1.2;
    color: #1a1a1a;
    max-width: 26em;
    text-align: center;
  }

  .studio-intro__text .nowrap {
    white-space: nowrap;
  }

  .studio-intro__sub {
    margin: 0;
    font-family: "ObviouslyWide", system-ui, sans-serif;
    font-size: clamp(11px, 0.85vw, 14px);
    font-weight: 400;
    line-height: 1.65;
    color: rgba(26, 26, 26, 0.55);
    max-width: 52ch;
    text-align: center;
  }
</style>
