---
---

<section class="studio-intro">
  <p class="studio-intro__text">Reference is an independent creative studio run like a band, trusted by the biggest names, built for work that sets the benchmark.</p>
</section>

<style>
  .studio-intro {
    background: #e8e4de;
    min-height: 100svh;
    padding: clamp(60px, 10vw, 140px) clamp(24px, 7vw, 100px);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .studio-intro__text {
    margin: 0;
    font-family: "ObviouslyWide", system-ui, sans-serif;
    font-size: clamp(22px, 3.6vw, 58px);
    font-weight: 400;
    line-height: 1.2;
    color: #1a1a1a;
    max-width: 26em;
    text-align: center;
  }
</style>
